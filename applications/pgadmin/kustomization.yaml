apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pgadmin
resources:
  - namespace.yaml
  - secrets.yaml
  - storage.yaml
  - deployment.yaml
  - service.yaml
  - ingress-route.yaml
configMapGenerator:
  - name: pgadmin-config
    files:
      - config/servers.json

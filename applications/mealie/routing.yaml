apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-route
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`mealie.wesl.io`)
      kind: Rule
      services:
        - name: service
          port: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  selector:
    app: mealie
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090

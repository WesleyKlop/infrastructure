apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mealie
resources:
  - namespace.yaml
  - deployment.yaml
  - routing.yaml
  - secret.yaml
  - storage.yaml

configMapGenerator:
  - name: mealie-config
    envs:
      - mealie-config.env

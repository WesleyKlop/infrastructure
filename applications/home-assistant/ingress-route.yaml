apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-route
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`ha.wesl.io`)
      kind: Rule
      services:
        - name: home-assistant
          port: http

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home-assistant
resources:
  - namespace.yaml
  - helm-release.yaml
  - helm-repo.yaml
  - ingress-route.yaml
configurations:
  - kustomizeconfig.yaml
configMapGenerator:
  - name: values
    files:
      - values.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik
resources:
  - setup.yaml
  - storage.yaml
  - namecheap-api.yaml
  - helmrelease.yaml
  - helmrepo.yaml
configurations:
  - kustomizationconfig.yaml
configMapGenerator:
  - name: traefik-config
    files:
      - values.yaml
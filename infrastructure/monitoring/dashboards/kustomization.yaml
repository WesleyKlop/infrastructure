apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: traefik
    files:
      - traefik.json
      - pi-hole.json
      - flux-cluster.json
      - flux-control-plane.json
generatorOptions:
  labels:
    grafana_dashboard: '1'

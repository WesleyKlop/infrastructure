apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # This should first be deployed as the basis of all applications
  - "../argo-cd/idris"
  # This self-manages argocd
  - ./argo-cd.yaml
  # This deploys cluster-specific resources
  - ./cluster-resources.yaml
  # Secret management operators
  - ./1password.yaml
  - ./external-secrets.yaml

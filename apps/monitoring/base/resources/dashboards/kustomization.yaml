apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - files:
      - traefik.json
    name: traefik
  - files:
      - argocd.json
    name: argo-cd

generatorOptions:
  labels:
    grafana_dashboard: '1'

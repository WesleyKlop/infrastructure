apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - files:
      - pi-hole.json
    name: pi-hole
  - files:
      - zfs.json
    name: zfs
  - files:
      - metallb.json
    name: metallb

generatorOptions:
  labels:
    grafana_dashboard: '1'

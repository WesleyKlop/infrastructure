apiVersion: v1
kind: Service
metadata:
  name: external-dns
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: external-dns
  ports:
    - name: http
      port: 7979
      targetPort: http
      protocol: TCP

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-dns

resources:
  - resources/namespace.yaml
  - https://github.com/kubernetes-sigs/external-dns//kustomize/?ref=v0.12.2

patchesStrategicMerge:
  - "patches/deployment.yaml"

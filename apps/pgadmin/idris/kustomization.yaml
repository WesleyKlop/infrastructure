apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pgadmin
resources:
  - ../base
  - ./secrets.yaml
  - ./storage.yaml
  - ./ingress-route.yaml
configMapGenerator:
  - name: pgadmin-config
    files:
      - config/servers.json
      - config/config_local.py

apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: metallb
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - metallb-system
  selector:
    matchLabels:
      app: metallb
  podMetricsEndpoints:
    - port: monitoring

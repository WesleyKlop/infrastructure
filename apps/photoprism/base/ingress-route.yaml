apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: photoprism
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`photos.wesl.io`)
      kind: Rule
      services:
        - name: photoprism
          port: 80

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - ./storage.yaml
  - ./secret.yaml

configMapGenerator:
  - name: photos-wesl-io-config
    envs:
      - env.conf

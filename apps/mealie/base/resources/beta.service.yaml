apiVersion: v1
kind: Service
metadata:
  name: mealie-beta
spec:
  selector:
    app.kubernetes.io/version: beta
  ports:
    - protocol: TCP
      port: 9000
      targetPort: api
      name: api
    - protocol: TCP
      port: 3000
      targetPort: web
      name: web

apiVersion: v1
kind: Service
metadata:
  name: mealie
spec:
  ports:
    - protocol: TCP
      port: 9000
      targetPort: api
      name: api
    - protocol: TCP
      port: 3000
      targetPort: web
      name: web

apiVersion: v1
kind: Service
metadata:
  name: mealie
spec:
  ports:
    - protocol: TCP
      targetPort: web
      port: 3000
      name: web
    - protocol: TCP
      targetPort: api
      port: 9000
      name: api
